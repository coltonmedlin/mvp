import React from 'react';
import ReactDOM from 'react-dom';

const ItemAdd = (props) => (
  <div>
    <form>
    <input type="text" label="item" /><button>add</button>
    </form>
  </div>
)

export default ItemAdd